[pytest]
# Pytest configuration for ChEMBL 3D Explorer

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Test paths
testpaths = tests

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests

# Timeout (in seconds) - useful for tests that might hang
# timeout = 300

# Minimum Python version
minversion = 3.10
